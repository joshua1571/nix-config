#!/usr/bin/env bash
# Runs the same checks as CI (see .github/workflows/ci.yml) before allowing a commit.
#
# One-time setup: git config core.hooksPath .githooks

set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/checks.sh" all
